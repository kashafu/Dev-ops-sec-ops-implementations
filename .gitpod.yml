image:
  file: .gitpod.Dockerfile

tasks:
  - command: docker-compose up

ports:
    - name: Backend
      port: 8000
      onOpen: open-browser
    
github:
    prebuilds:
        master: true
        pullRequests: false
        addComment: false